# 📋 دليل إعداد قاعدة البيانات - POVA

## ترتيب تنفيذ الملفات SQL

قم بتنفيذ الملفات بالترتيب التالي في **SQL Editor** في Supabase:

---

### ✅ المرحلة 1: Extensions والجداول الأساسية
**الملف:** `01_extensions_and_tables.sql`

**المحتوى:**
- تفعيل UUID extension
- إنشاء 8 جداول أساسية:
  - categories (الفئات)
  - subcategories (الفئات الفرعية)
  - services (المنتجات/الخدمات)
  - product_sizes (أحجام المنتجات)
  - product_images (صور المنتجات)
  - banners (البانرات)
  - store_settings (إعدادات المتجر)
  - testimonials (الشهادات)

---

### ✅ المرحلة 2: الجداول الإضافية
**الملف:** `02_additional_tables.sql`

**المحتوى:**
- إنشاء 8 جداول إضافية:
  - profiles (ملفات المستخدمين)
  - orders (الطلبات)
  - order_items (تفاصيل الطلبات)
  - coupons (الكوبونات)
  - product_reviews (مراجعات المنتجات)
  - wishlist (قائمة المفضلة)
  - cart_items (عربة التسوق)
  - downloads (التحميلات)

---

### ✅ المرحلة 3: الفهارس (Indexes)
**الملف:** `03_indexes.sql`

**المحتوى:**
- إنشاء جميع الفهارس لتحسين الأداء

---

### ✅ المرحلة 4: الدوال والـ Triggers
**الملف:** `04_functions_and_triggers.sql`

**المحتوى:**
- دالة تحديث updated_at
- دالة إنشاء رقم طلب فريد
- دالة إنشاء ملف تعريف المستخدم
- جميع الـ Triggers

---

### ✅ المرحلة 5: RLS وسياسات الأمان
**الملف:** `05_rls_and_policies.sql`

**المحتوى:**
- تفعيل Row Level Security على جميع الجداول
- إنشاء جميع سياسات الأمان

---

### ✅ المرحلة 6: البيانات الافتراضية
**الملف:** `06_default_data.sql`

**المحتوى:**
- إدراج إعدادات المتجر الافتراضية لـ POVA
- إضافة تعليقات للتوثيق

---

### ✅ المرحلة 7: إنشاء Storage Buckets
**الملف:** `07_storage_buckets.sql`

**المحتوى:**
- إنشاء 6 Storage Buckets:
  - `images` (صور عامة - Public)
  - `products` (صور المنتجات - Public)
  - `banners` (البانرات - Public)
  - `downloads` (التحميلات - Private)
  - `avatars` (الصور الشخصية - Public)
  - `designs` (ملفات التصميم - Private)

---

### ✅ المرحلة 8: سياسات Storage Buckets
**الملف:** `08_storage_policies.sql`

**المحتوى:**
- إنشاء جميع سياسات الأمان لـ Storage Buckets

---

## 📝 خطوات التنفيذ

1. افتح [Supabase Dashboard](https://supabase.com/dashboard)
2. اختر مشروعك
3. اذهب إلى **SQL Editor**
4. نفذ الملفات بالترتيب من 01 إلى 08
5. بعد كل ملف، تحقق من عدم وجود أخطاء

---

## ⚠️ ملاحظات مهمة

- **الترتيب مهم جداً** - لا تغير ترتيب الملفات
- إذا ظهرت أخطاء "already exists" - هذا طبيعي، يعني الجدول موجود مسبقاً
- بعد المرحلة 6، تحقق من وجود إعدادات المتجر في جدول `store_settings`
- بعد المرحلة 7 و 8، تحقق من وجود 6 Buckets في **Storage**

---

## ✅ التحقق من النجاح

بعد تنفيذ جميع المراحل:

1. **Table Editor** → يجب أن ترى 16 جدول
2. **Storage** → يجب أن ترى 6 Buckets
3. **SQL Editor** → لا توجد أخطاء

---

## 🎉 تم الإعداد بنجاح!

الآن قاعدة البيانات جاهزة للاستخدام مع موقع POVA.


